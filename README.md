# docgen-ai

> AI-powered codebase documentation generator. Analyzes repos via AST parsing and generates architecture docs, API references, module guides, and onboarding material.

[No API key configured — summaries unavailable]

## Technology Stack

**Languages:**

- Typescript — 29 files (94%)
- Python — 2 files (5%)
- Javascript — 2 files (1%)

**Frameworks & Libraries:**

- Express

**Key Dependencies:**

- **Framework:** express

## Getting Started

### Prerequisites

- Node.js >= 18.0.0
- Python >= 3.8

### Installation

```bash
# Clone the repository
git clone <repository-url>
cd docgen-ai

# Install dependencies
npm install

# Install Python dependencies
pip install -r requirements.txt
```

### Environment Variables

Create a `.env` file in the project root:

```env
ANTHROPIC_API_KEY=<required>
DOCGEN_AI_KEY=<required>
DOCGEN_AI_PROVIDER=<required>
DOCGEN_AI_MODEL=<required>
DOCGEN_OUTPUT_DIR=<required>
DOCGEN_LOG_LEVEL=<required>
WAREHOUSE_API_URL=<required>
PAGE_SIZE=<required>
```

### Running the Application

```bash
# Development
npm run dev

# Production
npm start
# Run the application
python main.py
```

## API Overview

This project exposes 13 API endpoints.

| Method | Path | Handler | Auth |
|--------|------|---------|------|
| `GET` | `*` | anonymous | — |
| `GET` | `/users` | anonymous | 🔒 |
| `GET` | `/users/:id` | anonymous | 🔒 |
| `POST` | `/users` | anonymous | 🔒 |
| `PUT` | `/users/:id` | anonymous | 🔒 |
| `DELETE` | `/users/:id` | anonymous | 🔒 |
| `GET` | `/` | list_products | — |
| `GET` | `/{product_id}` | get_product | — |
| `POST` | `/` | create_product | 🔒 |
| `PUT` | `/{product_id}` | update_product | 🔒 |
| `DELETE` | `/{product_id}` | delete_product | 🔒 |
| `GET` | `/{product_id}/stock` | check_stock | — |
| `POST` | `/{product_id}/restock` | restock_product | 🔒 |

## Project Structure

```
root/  (2 files)
ai/  (1 files)
cli/  (1 files — api)
config/  (1 files — config)
core/analyzers/  (1 files)
core/cache/  (1 files)
core/generators/  (8 files)
core/parsers/  (4 files)
core/  (1 files)
types/  (1 files)
utils/  (2 files — utility)
tests/__fixtures__/  (5 files — test)
tests/helpers/  (1 files — utility)
tests/parsers/  (4 files — test)
```

## Infrastructure

- **.github/workflows** — GitHub Actions CI/CD
- **tsconfig.json** — TypeScript config

## Testing

Testing framework: jest, ts-jest

```bash
npm test
pytest
```

---

*Documentation generated by [docgen](https://github.com/docgen) on 2026-03-22*